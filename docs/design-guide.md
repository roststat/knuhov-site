# Дизайн-гайд — Кнухов консалтинг

Основан на референсе strangehelix (значения сняты с живого сайта, точные). Система: строгий чёрно-белый, один шрифт без серифа, сквозной приём «один скруглённый угол». Контент и структура сайта — из ТЗ; здесь только визуальный язык.

---

## 1. Фирменный приём (главный токен)

**Один скруглённый угол — нижний правый, 24px; остальные три угла строго 0px.** Это не деталь кнопки, а сквозное правило: применяется ко всему прямоугольному с радиусом — меню, кнопки, карточки, плашки.

- `border-radius: 0 0 24px 0;` (TL 0 · TR 0 · BR 24px · BL 0)
- Полная граница `1px solid #000000` на всех сторонах (рамка полная, асимметричен только радиус).
- Логотип — мозаика из квадратов, у каждого свой угол скруглён по-разному; тот же приём повторяется в меню, кнопках и текстуре подвала. Это носитель узнаваемости.

---

## 2. Цвет

Строго ч/б, без акцентного цвета.

| Токен | Значение |
|---|---|
| Текст на светлом | `#000000` |
| Текст на тёмном | `#FFFFFF` |
| Фон страницы | `#FFFFFF` |
| Фон подвала | `#000000` (настоящий чёрный, не тёмно-серый) |
| Тени | нет вообще (`box-shadow: none`) |
| Градиенты | нет |
| Акцент бренд-нейма | `#d02026` — только для слова «Dr.» в лок-апе логотипа, больше нигде |

> Цветовой акцент гайдлайном не предусмотрен для контента. Единственное исключение — красный «Dr.» в логотипе (часть нейминга, не UI-сигнал). Если позже захотим «сигнал потери» — это будет осознанное расширение, а не часть системы.

---

## 3. Шрифт

**Один шрифт везде.** Эталон референса — Roobert (Displaay, платный), геометричный гротеск среднего веса. Для бренда Кнухов выбран **Space Grotesk** (бесплатный, Google Fonts): геометричный, угловатый, средний вес (400–500) — заголовки тоже на ~500, не на жирном. На реальном сайте ближе всего к Roobert лягут General Sans или Switzer (Fontshare) — их и поставим в Claude Code; Space Grotesk — ближайшая замена, грузящаяся в превью. Никакого смешения sans + serif — только один гротеск, меняются вес и кегль (вес держим средний, без 800–900).

- Навигация и кнопки: **18px, weight 500, letter-spacing −0.18px**, цвет `#000`.
- Заголовки — тот же шрифт, просто очень крупно.

---

## 4. Меню (header)

- `position: fixed; top: 0; z-index: 100;` — плавает, не уезжает при скролле, не сжимается.
- Ширина 100% (полноширинная панель, не «остров» по центру).
- Высота **70px** (токен `--header-h: 4.375rem`), padding `0 24px`.
- Фон `#fff`, граница `1px solid #000`, без тени.
- Углы: только нижний-правый 24px, остальные 0.
- Пункты навигации 18px / 500, чёрные.

---

## 5. Кнопки

Наследуют логику углов меню — системное решение.

- Углы: только нижний-правый 24px, остальные 0.
- Граница `1px solid #000`, padding ~24px, текст 18px / 500.
- **Secondary:** белый фон, чёрный текст («Пройти диагностику»).
- **Dark:** чёрный фон, белый текст («Заказать аудит»).
- Без теней, без других скруглений.

---

## 6. Подвал (footer)

- Фон `#000000`.
- **Верхняя граница — текстура из ряда квадратов с разными скруглёнными углами** (та же мозаика, что в логотипе, масштабированная на ширину). Структурный паттерн бренда, не картинка.
- Контент слева → справа: белый логотип + 2 предложения о бренде + строка благодарности команде; крупная ссылка-подпись (два слова, на отдельных строках) с **толстым подчёркиванием** (жирная горизонтальная черта, не `text-decoration`); колонка навигации (повтор меню); иконки соцсетей; кнопка «Help & Support» — фирменная капсула в белой обводке на чёрном.
- Нижняя строка (Политика / Наверх / Условия) — мелкий текст, разделители — **крошечные квадраты**, не круглые точки.

---

## 7. Дополнительные приёмы

- Толстое подчёркивание у крупных ссылок-подписей.
- Квадратные точки-разделители (маленький квадрат).
- Никаких теней и градиентов нигде.

---

## 8. Сводная таблица токенов

| Элемент | Значение |
|---|---|
| Шрифт | Space Grotesk (средний вес 400–500; на сайте ближе к Roobert — General Sans / Switzer) |
| Цвет текста | `#000` на светлом, `#fff` на чёрном |
| Фон страницы | `#ffffff` |
| Фон подвала | `#000000` |
| Радиус (фирменный) | только нижний-правый `24px`, остальные `0` |
| Граница кнопок/меню | `1px solid #000` |
| Высота меню | `~80px`, `fixed`, `top: 0` |
| Кегль навигации/кнопок | `18px`, `weight 500`, `letter-spacing −0.18px` |
| Тени / градиенты | нет |
| Доп. приёмы | толстое подчёркивание; квадратные точки-разделители |

---

## 9. Голос (из ТЗ — не меняется)

Разделение «мы» (компания) / «я» (Ислам); sentence case; слово «агентство» не используем — только «консалтинг».

---

## 10. Привести в соответствие

- **Маршрутизатор и лента слоёв:** убрать цветовую подсветку — состояния в ч/б через чёрную заливку/инверсию и угловой приём.

---

## 11. Логотип (актуальная версия, текст + знак)

Растровых файлов (`logo.png`/`logo-white.png`) больше нет — лого собирается из HTML/CSS, чтобы жил на любом фоне и не требовал экспорта картинки при правках.

**Лок-ап:** знак-мозаика → «Dr.Консалтинг» (мелкая подпись) → «Knuhov.ru» (крупный домен-неймтег). Порядок именно такой — подпись перед доменом, не наоборот.

```html
<a class="brand" href="index.html#top" aria-label="Dr.Консалтинг — Knuhov.ru">
  <span class="mark mosaic" aria-hidden="true">
    <span style="border-radius:8px 0 0 0"></span>
    <span style="border-radius:0 0 0 8px"></span>
    <span class="tall" style="border-radius:0 8px 8px 0"></span>
  </span>
  <span class="brand-text">
    <span class="brand-kicker"><span class="brand-dr">Dr.</span>Консалтинг</span>
    <span class="brand-domain">Knuhov.ru</span>
  </span>
</a>
```

На тёмном фоне (подвал) добавить модификаторы: `.brand--footer` на `<a>`, `.mark--light` на знаке, `.brand-kicker--light` и `.brand-domain--light` на тексте — «Dr.» остаётся красным в обоих случаях.

**Знак — фирменная мозаика квадратов**, не закрашенный квадрат. Это единственный графический актив бренда, переиспользуется в логотипе и в блоке цитаты (`.quote-mark`). Базовый CSS-компонент:

```css
.mosaic{display:grid;grid-template-columns:1fr 1fr;grid-template-rows:1fr 1fr;flex:none}
.mosaic span{background:currentColor}
.mosaic .tall{grid-row:1 / span 2}
```

Контекст задаёт только `width/height/gap/color` через модификатор (`.mark` = 34×34, gap 4px, цвет `var(--ink)`; `.quote-mark` = 31×31, gap 3px). Один и тот же набор из 3 `<span>` с инлайн `border-radius` переносится без изменений между контекстами.

---

## 12. Переиспользуемые примитивы (чтобы не начинать с нуля на новых страницах)

Готовая библиотека классов в `styles.css` — бери их, а не пиши новые с нуля:

| Класс | Назначение |
|---|---|
| `.container` / `.container--wide` | Обёртка контента, левый/правый паддинг 100px |
| `section` | Базовый вертикальный ритм `110px 0` (адаптив `72px 0`) |
| `.btn`, `.btn--dark`, `.btn--sm` | Кнопка с фирменным углом, инверсия по hover |
| `.kicker` | Мелкий аплкейс-лейбл (используется и в `.brand-kicker`) |
| `.lead` | Подзаголовок-лид, серый, max-width 520px |
| `.mosaic` + модификатор размера | Фирменный знак (см. §11) |
| `.sqdot` | Квадратная точка-разделитель (вместо круглой) |
| `.product-card`, `.product-grid` | Карточки-продукты с hover-инверсией |
| `.popup`, `.popup_panel` | Модалка (использована в «Маршрутизаторе») |
| `--radius`, `--radius-sm`, `--radius-xs` | Фирменный угол на 3 масштаба (24/12/8px) |

Новую страницу собирать из этих классов первым делом; новый CSS писать только если в библиотеке реально нет подходящего примитива.
